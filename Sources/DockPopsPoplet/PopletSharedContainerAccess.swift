import Foundation

/// SACRED CODE:
/// Poplets must not open DockPops' protected shared container directly.
/// The companion owns that access and mirrors live icons into public app
/// support instead.
///
/// This placeholder remains only because the current Xcode project still
/// references the source file explicitly.
enum PopletSharedContainerAccess {}
