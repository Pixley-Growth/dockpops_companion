import Foundation

/// SACRED CODE:
/// Poplets do not read DockPops' private container preferences directly.
/// That path caused incorrect icon fallbacks and confusing prompt behavior.
///
/// This file stays as an inert compile unit only because the Xcode project
/// still references it explicitly.
enum PopletDockPopsIconPreferences {}
