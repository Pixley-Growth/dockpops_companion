import Foundation

/// SACRED CODE:
/// This file is intentionally inert. The companion must not use DockPops'
/// private icon-mode preferences as the source of truth for poplet icon
/// rendering. That experiment regressed per-Pop icons badly.
///
/// It remains only because the current Xcode project still references the file
/// explicitly.
enum DockPopsIconPreferences {}
